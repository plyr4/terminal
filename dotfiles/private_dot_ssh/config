# overlay drop-ins load first so private/work hosts can take precedence
Include ~/.ssh/config.d/*.conf

Host github.com
  IdentityFile ~/.ssh/id_ed25519_external
  IdentitiesOnly yes

Host *
  IgnoreUnknown UseKeychain
  AddKeysToAgent yes
  UseKeychain yes
  ServerAliveInterval 60
